#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;









/*struct bao
{
	int v;
	int w;
}data[1000];

int N,M;
int ans;
int rest_v;
void dfs(int m,int w)
{
	if(m == N)
	{
		if(ans < w)
		{
			ans = w;
		}
		return;
	}

	if(rest_v > data[m].v)
	{
		dfs(m + 1, w + data[m])
		dfs(m + 1, w);
	}
	else
	{
		dfs(m + 1, w);
	}
}

int main()
{
	cin>>N,M;
	rest_v = M;
	for(int i = 0 ; i < N; i++)
	{
		cin>>data[i].v>>data[i].m=w;
	}

	dfs(0,0);
	cout<<ans;
}*/